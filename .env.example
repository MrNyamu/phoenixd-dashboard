# =============================================================================
# PhoenixD Client Environment Configuration
# =============================================================================
# Copy this file to .env.local and update the values below with your actual
# PhoenixD configuration. Never commit .env.local to version control.

# -----------------------------------------------------------------------------
# PhoenixD Connection Settings (Required)
# -----------------------------------------------------------------------------

# The URL where your PhoenixD node is running
# Default: http://localhost:9740
# For Docker: http://phoenixd:9740
# For remote nodes: https://your-domain.com:9740
NEXT_PUBLIC_PHOENIXD_URL=http://localhost:9740

# PhoenixD authentication username
# This is usually 'phoenix' by default for PhoenixD
NEXT_PUBLIC_PHOENIXD_USERNAME=phoenix

# PhoenixD authentication password
# IMPORTANT: Replace this with your actual PhoenixD password!
# You can find this in your PhoenixD configuration or by running:
# docker exec phoenixd cat /phoenix/.phoenix/phoenix.conf
NEXT_PUBLIC_PHOENIXD_PASSWORD=your-phoenixd-password-here

# -----------------------------------------------------------------------------
# Webhook Configuration (Optional)
# -----------------------------------------------------------------------------

# Secret key for webhook authentication (optional)
# Generate a secure random string if you plan to use webhooks
# You can generate one with: openssl rand -hex 32
PHOENIXD_WEBHOOK_SECRET=your-webhook-secret-here

# -----------------------------------------------------------------------------
# Network Configuration (Optional)
# -----------------------------------------------------------------------------

# Bitcoin network: mainnet or testnet
# Default: testnet (safer for development)
NEXT_PUBLIC_PHOENIX_NETWORK=testnet

# -----------------------------------------------------------------------------
# Next.js Configuration (Optional)
# -----------------------------------------------------------------------------

# Disable Next.js telemetry data collection
NEXT_TELEMETRY_DISABLED=1

# Development mode configuration
# NODE_ENV=development

# -----------------------------------------------------------------------------
# Lightning Address Configuration (Optional)
# -----------------------------------------------------------------------------

# Domain for Lightning addresses (if using Lightning address feature)
# LIGHTNING_ADDRESS_DOMAIN=yourdomain.com

# Lightning address admin email
# LIGHTNING_ADDRESS_ADMIN_EMAIL=admin@yourdomain.com

# -----------------------------------------------------------------------------
# Database Configuration (Optional - for future features)
# -----------------------------------------------------------------------------

# Database connection string (if implementing persistent storage)
# DATABASE_URL=postgres://user:password@localhost:5432/phoenixd_client

# Redis connection (if implementing caching)
# REDIS_URL=redis://localhost:6379

# -----------------------------------------------------------------------------
# Production Configuration (Optional)
# -----------------------------------------------------------------------------

# Next.js build output configuration
# NEXT_OUTPUT=standalone

# Custom port for production deployment
# PORT=3000

# -----------------------------------------------------------------------------
# Security Configuration (Optional)
# -----------------------------------------------------------------------------

# CORS allowed origins (comma-separated)
# ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# Rate limiting configuration
# RATE_LIMIT_REQUESTS_PER_MINUTE=60

# -----------------------------------------------------------------------------
# Instructions:
# -----------------------------------------------------------------------------
# 1. Copy this file: cp .env.example .env.local
# 2. Update NEXT_PUBLIC_PHOENIXD_PASSWORD with your actual PhoenixD password
# 3. Adjust NEXT_PUBLIC_PHOENIXD_URL if your PhoenixD runs elsewhere
# 4. Generate a secure PHOENIXD_WEBHOOK_SECRET if using webhooks
# 5. Set NEXT_PUBLIC_PHOENIX_NETWORK to 'mainnet' for production
# 6. Never commit .env.local to version control!
#
# To find your PhoenixD password:
# - Check your PhoenixD logs on first startup
# - Look in phoenix.conf file
# - For Docker: docker logs phoenixd | grep -i password
#
# For production deployment:
# - Use environment variables instead of .env files
# - Enable HTTPS for NEXT_PUBLIC_PHOENIXD_URL
# - Use strong, unique passwords and secrets
# - Consider using a secrets management service